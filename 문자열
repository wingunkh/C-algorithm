/*문자열이란?
: 프로그램에서 '문자의 나열'을 나타낸다. 문자의 나열을 두개의 큰따옴표로 묶는 것을 '문자열 리터럴'이라고 한다.
이때, 컴퓨터는 문자열 리터럴의 끝을 나타내기 위해 널 문자('\0')를 자동으로 추가한다.
많은 C언어 프로그램에서는 문자열을 배열이 아니라 포인터로 표현하는데,
이때 포인터는 배열과 다르게 문자열 리터럴을 저장하기 위한 영역 외에도 포인터변수가 갖는 메모리 영역이 더 필요하다는걸 유의한다.
*/

#include <stdio.h>

void swap_prt(char **x, char **y){ //포인터를 가리키는 포인터이므로 '**변수이름' 으로 선언한다. 문자열 리터럴을 가리키는 두 포인터의 값을 교환하는 함수이다.
	char *tmp=*x; //tmp라는 포인터변수에 x가 가리키는 값을 대입하는데, 이때 x가 가리키는 값이 주소이므로 문법 상 올바르다. 
	*x=*y;
	*y=tmp; //그렇기 때문에 y가 가리키는 값(=주소)에 tmp가 가리키는 값(=주소) 를 대입하는데, 문법 상 올바르다.
}

int main(void){
	char *s1="ABCD";
	char *s2="EFGH";
	
	printf("포인터 s1은 \"%s\"를 가리킵니다.\n",s1);
	printf("포인터 s2은 \"%s\"를 가리킵니다.\n",s2);
	
	
	printf("\n포인터 s1과 s2의 값을 서로 교환했습니다.\n\n");
	
	swap_prt(&s1,&s2);
	
	printf("포인터 s1은 \"%s\"를 가리킵니다.\n",s1);
	printf("포인터 s2은 \"%s\"를 가리킵니다.\n",s2);
	
	return 0;
}
